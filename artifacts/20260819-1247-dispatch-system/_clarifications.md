# 需求澄清清单

## 文档成熟度

**判定：** 粗糙（有需求但粗糙）

**策略：** 需求已覆盖调度系统的业务目标、主要模块、角色权限和部分量化指标，但存在本期范围互相冲突、核心状态转换未闭环、MOBI 与页面双写规则缺失、金额及删除口径不完整等问题。应先确认阻塞级事项并将结论回填需求文档，再补齐异常、边界和依赖口径后冻结测试范围。

---

## 🔴 阻塞级（必须先澄清才能写用例）

### 异常分支

| # | 来源（页面/字段） | 问题 | 影响范围 | 建议澄清方式 |
|---:|---|---|---|---|
| 1 | 任务管理－MOBI 后台同步与页面新增 | 两个入口可同时创建任务，但未定义唯一键、幂等、重复数据和同步覆盖规则。 | 任务新增、同步、编辑、统计、成本关联及数据一致性用例 | 请确认 MOBI 同步任务与页面新增任务分别使用什么唯一键和来源标识；同一任务重复到达或两端并发写入时，是拒绝、合并还是覆盖，冲突由谁处理？ |
| 2 | 登录注册－注册后待激活 | 未定义管理员拒绝、审批超时、通知失败及重复提交的处理。 | 注册、激活、登录、用户管理和安全用例 | 请确认待激活账号的审批人、通过和拒绝后的账号状态及通知方式；审批超时、通知发送失败或用户重复注册时系统应如何处理？ |
| 3 | 成本管理－确认成本 | 已确认不可编辑，但确认失败、部分成功及误确认后的修正路径缺失。 | 单条确认、批量确认、财务对账、统计和审计用例 | 请确认成本确认接口失败或批量处理中部分记录失败时是否整体回滚；误确认后允许撤销、冲销还是新建调整单，分别需要什么权限？ |
| 4 | 系统管理－用户体系 RPC | 沿用现有用户体系，但 RPC 超时、失败、返回冲突数据时的交互和补偿未定义。 | 用户增删改查、登录、权限生效及异常恢复用例 | 请确认用户管理 RPC 超时、不可用或返回账号冲突时，页面应如何提示、是否重试、是否保留已填内容，以及本地是否允许产生待同步数据？ |

### 边界值规则

| # | 来源 | 问题 | 影响范围 | 建议澄清方式 |
|---:|---|---|---|---|
| 5 | 船舶与设备－客户船管理 | 功能清单列为 P0 且在范围内，详细备注却写“本期不做”。 | 本期范围、导航、权限、任务字段、客户船 CRUD 与回归边界 | 请明确客户船管理本期最终是否交付；如果不交付，任务中的客户船数据由哪里维护和选择，相关入口、权限及用例是否全部移出本期？ |
| 6 | 成本管理－批量确认 | 功能清单列为 P1，详细备注称本期暂不处理。 | 版本范围、成本列表操作、批量异常及权限用例 | 请明确成本批量确认是否属于本期验收范围；若不属于，请确认页面是否隐藏入口，以及本期是否只保留单条确认？ |
| 7 | 设备管理－设备状态 | 同时出现“可用、不可用、维护中、故障”和 `available/unavailable` 两套枚举。 | 设备 CRUD、筛选、船舶联动、接口契约和数据迁移用例 | 请给出机器人、遥控箱和遥控手柄的最终状态枚举、接口值及中文文案，并明确两套现有口径的映射关系和历史数据迁移规则。 |
| 8 | 成本管理－金额与汇率 | 固定汇率与“自动换算”目标并存，金额精度、范围、舍入和生效版本均未定义。 | 成本录入、换算、确认、汇总、导出及准确率验收用例 | 请明确本期采用固定汇率还是外部动态汇率，并给出金额最小值、最大值、小数位、负数和零是否允许、人民币折算舍入方式及汇率生效时间。 |

### 状态机细节

| # | 来源 | 问题 | 影响范围 | 建议澄清方式 |
|---:|---|---|---|---|
| 9 | 任务管理－作业状态枚举 | 有待开始、待定、已取消、维修、已派单、已完成、客户参观，首页又使用规划中和进行中，缺少转换矩阵。 | 新增、编辑、分配、首页待办、统计、删除和只读用例 | 请提供任务状态完整转换表，明确每个状态的进入条件、可执行角色、允许的下一状态及能否回退，并说明首页“规划中、进行中”与后台状态的映射。 |
| 10 | 作业船与设备－状态联动 | 仅说明船卡状态与设备状态联动，触发方向、组合规则和失败回滚不明。 | 任务分配、船舶状态、设备占用、维修和并发用例 | 请明确任务、作业船、机器人、遥控箱和遥控手柄之间的状态联动矩阵，包括谁触发谁、多个设备状态不一致时船舶取什么状态、联动失败时如何回滚。 |
| 11 | 用户账号－待激活、启用、禁用、锁定 | 审批、启停和登录锁定各自存在，但状态优先级、解锁及会话处理不明。 | 注册、审批、登录失败、用户启停和会话安全用例 | 请给出用户账号完整状态机，并明确待激活、禁用和登录锁定能否同时存在、状态优先级、自动或人工解锁条件，以及禁用后已有会话何时失效。 |
| 12 | 成本管理－待确认与已确认 | 只规定已确认不可编辑，未定义待确认来源、确认权限、撤销和终态修正。 | 成本新增、编辑、确认、待确认金额卡片、统计与审计用例 | 请给出成本记录从创建到确认、撤销、冲销或作废的完整状态流，明确各转换角色、前置条件及已确认数据的合法修正方式。 |

### 隐性依赖

| # | 来源 | 问题 | 影响范围 | 建议澄清方式 |
|---:|---|---|---|---|
| 13 | 权限矩阵与注册表单－角色字段 | 注册表单包含角色且默认调度员，未说明用户能否自选管理员或财务。 | 注册安全、越权、审批、角色配置和权限验收用例 | 请确认注册用户是否可以选择角色；如可选择，哪些角色可选、是否需额外审批；如不可选择，是否应固定为调度员并隐藏角色字段？ |
| 14 | 任务管理－分组关联督导、队长、队员 | 分组是人员自动带出的依赖源，但分组数据来源、失效及人工覆盖规则缺失。 | 任务新增编辑、人员分配、休假离职联动和历史数据用例 | 请确认分组、督导、队长和队员的主数据来源及关联规则；成员休假、离职或分组变更时，未开始和进行中任务是否自动更新，是否允许人工覆盖？ |
| 15 | 数据保留－永久存储与删除功能 | 非功能要求“永久存储”，任务软删除，其他模块又支持删除，生命周期口径冲突。 | 全模块删除、恢复、统计、导出、审计、备份及合规用例 | 请明确各业务实体删除后是软删除、归档还是物理删除，是否支持恢复，删除数据是否参与查询和统计，以及“永久存储”适用于哪些数据和例外场景？ |
| 16 | 角色权限－全部、查看、无 | 仅有模块级矩阵，按钮、字段和数据范围依赖未说明。 | 导航、接口鉴权、增删改查、导出、越权及角色变更用例 | 请确认“全部、查看、无”在页面入口、按钮、字段、导出及接口层的具体含义，并明确权限变更后已登录用户何时生效及是否需要重新登录。 |

---

## 🟡 影响覆盖级（不澄清会影响异常或边界用例覆盖）

### 异常分支

| # | 来源（页面/字段） | 问题 | 影响范围 | 建议澄清方式 |
|---:|---|---|---|---|
| 17 | 全模块－新增、编辑、删除、确认、导出 | 未统一描述超时、4xx/5xx、断网、重复点击时的反馈与数据保留。 | 异常提示、防重复提交、重试和表单恢复用例 | 请确认全局操作在网络超时、4xx/5xx、断网及重复点击时的统一交互规范，包括按钮是否禁用、是否自动重试、已填数据是否保留和错误文案要求。 |
| 18 | 任务、成本和资源管理－多人编辑 | 未定义并发修改和版本冲突处理。 | 并发编辑、最后写入、刷新提示及审计用例 | 请确认两个用户同时编辑同一任务、成本或资源时采用乐观锁、悲观锁还是最后写入覆盖；发生版本冲突时页面应如何提示并保留用户修改？ |
| 19 | 多模块导出－CSV/PDF/Excel | 仅任务导出有编码和文件名规则，失败、超时及空数据行为未说明。 | 导出异常、重试、文件完整性和权限用例 | 请确认导出无数据、生成超时、下载中断或服务失败时是否生成空文件、如何提示和重试，以及是否允许同一用户并发发起多个导出任务？ |

### 边界值规则

| # | 来源 | 问题 | 影响范围 | 建议澄清方式 |
|---:|---|---|---|---|
| 20 | 登录注册－用户名、密码、邮箱 | 仅有部分长度规则，空格、字符集、大小写、邮箱格式和密码上限/复杂度不明。 | 输入校验、唯一性、安全及错误提示用例 | 请给出用户名、密码和邮箱的完整校验规则，包括最小和最大长度、允许字符、首尾空格处理、大小写是否敏感、字符计数方式及密码复杂度要求。 |
| 21 | 任务及资源－日期时间字段 | 时区、跨日、闰年、起止时间关系和空值规则未定义。 | 任务新增编辑、甘特图、排序、统计和导出用例 | 请确认所有业务时间统一使用哪个时区，开始时间等于或晚于结束时间是否允许，跨日和跨年任务如何展示与统计，以及日期时间字段是否允许为空？ |
| 22 | 列表－分页、搜索、筛选 | 默认页大小、越界页、大小写、组合筛选及空结果行为缺失。 | 任务、设备、队员、成本、日志等列表边界用例 | 请确认列表默认及可选页大小、最大可翻页数据量、越界页处理、搜索匹配和大小写规则，以及搜索、筛选、排序和翻页条件是否互相保留。 |

### 状态机细节

| # | 来源 | 问题 | 影响范围 | 建议澄清方式 |
|---:|---|---|---|---|
| 23 | 队员管理－活跃、休假、离职 | 休假结束、离职恢复及已有任务处理未定义。 | 队员编辑、任务分配、状态回退、绩效和删除用例 | 请确认队员从活跃进入休假或离职后，已分配但未开始及进行中的任务如何处理；休假能否自动或人工恢复活跃，离职能否恢复？ |
| 24 | 作业船管理－可用、作业中、维护中 | 状态转换触发者、占用冲突和维护结束路径缺失。 | 船舶维护、任务分配、并发占用和恢复用例 | 请确认作业船各状态由用户手工设置还是由任务自动驱动，并给出可用、作业中、维护中的合法转换、重复占用校验及维护结束后的目标状态。 |
| 25 | 角色管理－启用、禁用 | 禁用角色后其用户、已有会话及权限快照处理不明。 | 角色启停、登录态、接口鉴权和权限恢复用例 | 请确认禁用角色后关联用户能否登录、已有会话是否立即失效、历史数据如何展示，以及再次启用后原权限配置是否完整恢复？ |

### 隐性依赖

| # | 来源 | 问题 | 影响范围 | 建议澄清方式 |
|---:|---|---|---|---|
| 26 | 首页－天气预警 | 未说明天气数据提供方、刷新频率、区域匹配和服务不可用降级。 | 首页预警、外部接口、缓存、异常及数据时效用例 | 请确认天气预警的数据提供方、港口或作业区域匹配规则、刷新频率与缓存时长，并说明外部服务不可用或数据过期时页面如何降级展示。 |
| 27 | 成本与统计－汇总数据 | 成本确认后的统计刷新时点、历史汇率及更正回算规则缺失。 | 成本列表、待确认金额、周期成本、导出和对账用例 | 请确认成本确认、冲销或汇率调整后各统计卡片和图表何时刷新，历史记录是否按原汇率保留，还是按新汇率重新计算？ |
| 28 | 操作日志－登录、新增、编辑、删除、确认 | 日志字段、失败操作、保留期和敏感信息脱敏未定义。 | 审计、安全、查询、权限和数据保留用例 | 请确认操作日志需记录哪些主体、对象、前后值、时间、IP 和结果字段，失败操作是否记录、保留多久，以及密码和个人信息如何脱敏？ |

---

## 🟢 优化建议级（不阻塞，但澄清后可提升测试质量）

### 异常分支

| # | 来源（页面/字段） | 问题 | 影响范围 | 建议澄清方式 |
|---:|---|---|---|---|
| 29 | 首页及各列表－空数据和加载失败 | 除非调度员待办文案外，空态、无结果态和加载失败态均未定义。 | 首页、列表、图表的可用性和异常展示用例 | 请确认首页卡片、甘特图、列表和统计图表在首次无数据、筛选无结果、部分数据缺失及加载失败时分别显示什么文案、图形和可执行操作？ |
| 30 | 登录－会话超时 | 只规定返回登录页，未说明未保存内容与登录后回跳。 | 会话过期、表单恢复和导航连续性用例 | 请确认用户编辑表单时会话过期，系统是否保存草稿并提示；重新登录后是返回原页面恢复内容，还是统一进入首页？ |

### 边界值规则

| # | 来源 | 问题 | 影响范围 | 建议澄清方式 |
|---:|---|---|---|---|
| 31 | 任务新增/编辑弹窗－宽 1000px | 未定义最小视口、缩放和字段超长时的响应式规则。 | 桌面浏览器兼容、弹窗布局和可访问性用例 | 请确认任务弹窗支持的最小屏幕分辨率和浏览器缩放范围；视口小于 1000px 或字段内容超长时采用滚动、换行还是截断并显示提示？ |
| 32 | 非功能－多浏览器访问 | 未列浏览器、最低版本、分辨率和操作系统。 | 兼容性测试矩阵和验收范围 | 请列出本期必须验收的浏览器及最低版本、操作系统和桌面分辨率，并确认是否需要支持 Safari、Firefox 及高 DPI 缩放。 |

### 状态机细节

| # | 来源 | 问题 | 影响范围 | 建议澄清方式 |
|---:|---|---|---|---|
| 33 | 找回密码－链接有效 24 小时 | 一次性使用、重复申请和旧链接失效规则未说明。 | 找回密码、链接超时和安全用例 | 请确认密码重置链接是否仅可使用一次；用户重复申请或成功重置后，之前已发送且仍在 24 小时内的链接是否立即失效？ |
| 34 | 任务管理－软删除 | 删除后的恢复、唯一键占用和关联记录展示未描述。 | 删除恢复、再次创建、统计、成本和历史任务用例 | 请确认软删除任务是否支持恢复，删除后其 MOBI 或 Unique ID 能否再次创建，关联成本、人员历史和统计中应隐藏、保留还是标记为已删除？ |

### 隐性依赖

| # | 来源 | 问题 | 影响范围 | 建议澄清方式 |
|---:|---|---|---|---|
| 35 | 非功能－首屏、提交、导出、并发和可用性指标 | 已有数值但测试环境、百分位、持续时间和排除项缺失。 | 性能、容量和可用性验收用例 | 请确认首屏 3 秒、响应 1 秒、1000 条导出 5 秒、20 人并发及 99.5% 可用性的测试环境、网络条件、P95/P99 口径、持续时间和计划维护排除规则。 |
| 36 | 绩效分析－效率、评分和等级 | 效率公式已给出，但零时长、汇总平均权重及二期字段展示规则不明。 | 绩效计算、合计行、导出及本期页面展示用例 | 请确认作业时长为零或数据缺失时效率如何计算，合计行采用简单平均还是加权平均，以及本期尚未计算的评分和等级字段是隐藏、留空还是标注“二期”？ |

---

## 数量汇总

| 优先级 | 异常分支 | 边界值规则 | 状态机细节 | 隐性依赖 | 合计 |
|---|---:|---:|---:|---:|---:|
| 🔴 阻塞级 | 4 | 4 | 4 | 4 | 16 |
| 🟡 影响覆盖级 | 3 | 3 | 3 | 3 | 12 |
| 🟢 优化建议级 | 2 | 2 | 2 | 2 | 8 |
| **总计** | **9** | **9** | **9** | **9** | **36** |
